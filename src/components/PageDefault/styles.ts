
import styled from "styled-components";

export const Main = styled.main`
    flex: 1;
    padding: 5% 10%;
    min-height: 100vh;
    color: var(--white);
    @media (max-width: 800px) {
      margin-top: 40px;
    }
`;